<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.korit.allways_back.mapper.ItemMapper">
    <select id="getItemsByCategoryName" resultType="com.korit.allways_back.entity.Item">
        SELECT
            i.item_id,
            i.item_name,
            i.content,
            i.price,
            i.size,
            i.img_url,
            c.item_category_name
        FROM item_tb i
        INNER JOIN item_category_tb c
            ON i.item_category_id = c.item_category_id
        WHERE c.item_category_name = #{categoryName}
    </select>
</mapper>

