<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.korit.allways_back.mapper.PostMapper">
    <insert id="createPost" useGeneratedKeys="true" keyProperty="postId">
        INSERT INTO
            post_tb
        VALUES
            (default, #{presetId}, now(), #{likeCnt})
    </insert>
    <update id="incrementLikeCount">
        update post_tb
        set like_cnt = like_cnt + 1
        where post_id = #{postId}
    </update>

    <insert id="insertLikeLog">
        insert into like_tb
        values (#{userId}, #{postId})
    </insert>
    <select id="getAllPosts" resultType="com.korit.allways_back.entity.Post">
        select * from post_tb order by posted_at desc
    </select>

    <select id="checkLikeExists" resultType="int">
        select count(*) from like_tb where user_id = #{userId} and post_id = #{postId}
    </select>

    <delete id="deleteLikeLog">
        delete from like_tb where user_id = #{userId} and post_id = #{postId}
    </delete>

    <update id="decrementLikeCount">
        update post_tb set like_cnt = like_cnt - 1 where post_id = #{postId}
    </update>
</mapper>