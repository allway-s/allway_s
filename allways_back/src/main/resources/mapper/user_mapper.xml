<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.korit.allways_back.mapper.UserMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user_tb
        VALUES (
            default,
            #{oauth2Id},
            #{nickname},
            #{name},
            #{email},
            #{phoneNumber},
            now()
        )
    </insert>

    <select id="findByUserId" resultType="User">
        SELECT
            user_id,
            oauth2_id,
            nickname,
            name,
            email,
            phone_number,
            created_at
        FROM
            user_tb
        WHERE
            user_id = #{userId}
    </select>

    <select id="findByOauth2Id" resultType="User">
        SELECT
            user_id,
            oauth2_id,
            nickname,
            name,
            email,
            phone_number,
            created_at
        FROM
            user_tb
        WHERE
            oauth2_id = #{oauth2Id}
    </select>

    <select id="countByNickname" resultType="integer">
        SELECT
            COUNT(*)
        FROM
            user_tb
        WHERE
            nickname = #{nickname}
    </select>
</mapper>